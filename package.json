{
	"private": true,
	"scripts": {
		"build": "pnpm --recursive build",
		"watch": "pnpm --parallel --filter=\"./packages/*\" build --watch",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4"
	},
	"pnpm": {
		"overrides": {
			"vite": "6.0.0-beta.7"
		}
	}
}
